# Project_STD 命名改名清单（执行参考）

- **依据**：`docs/命名约定.md` v1.1
- **已裁决**：目录/文件缩为 `CASC`；符号前缀缩为 `app_casc_*`；`AH_MQTT`→`AHMQ`；LwIP/ST 名区分处理（`sys_now`/`sys_jiffies`/`heth` 豁免，`ethernetif_*`/`gnetif` 改 `pl_eth_*`）
- **规模**：约 2,250–2,350 行引用 + 约 15 个文件改名/移动
- **批次**：B1 符号级（`make test` 守门）→ B2 文件/目录级（双构建守门）。B1 内部建议按子系统分多个 commit 便于 `git show` 评审，但共用一次测试门槛。

---

## B1 符号级（含层前缀、消歧、归位、自注释）

### B1-A Kernel + Platform（约 276 行）

**Kernel —— 模块前缀（§1.1）**
- `text_cvt.h`：`chr2hex`→`cvt_chr_to_hex`、`hex2chr`→`cvt_hex_to_chr`、`HexToStr`→`cvt_hex_to_str`、`StrToHex`→`cvt_str_to_hex`、`GBKToUTF8`→`cvt_gbk_to_utf8`、`UTF8ToGBK`→`cvt_utf8_to_gbk`、`GBKToUnicode`→`cvt_gbk_to_unicode`、`UnicodeToGBK`→`cvt_unicode_to_gbk`、`UTF8ToUnicode`→`cvt_utf8_to_unicode`、`UnicodeToUTF8`→`cvt_unicode_to_utf8`
- `text_cvt.h:8`：`TEXTCODEC_ENABLE`→`CVT_TEXTCODEC_ENABLE`
- `text_cvt.c:384`：`convert_encoding`→`_cvt_convert_encoding`（补 `_`）
- `bit_utils.c:8`：`ctz_table`→`s_ctz_table`
- `bcc_utils.h:4`：`bcc_calcu`→`bcc_calc`
- `initcall.h:58`：`sw_board_init`→`initcall_run_sw`（段名 `.sw_initcall` 等 **不动**，§13）
- `initcall.h:23`：`initcall_fn`→`initcall_fn_t`

**Platform —— 回调 typedef（§8.1）**
- `pl_uart_rx_cb_t`→`pl_uart_rx_fn_t`、`pl_spi_rx_cplt_cb_t`→`pl_spi_rx_done_fn_t`、`pl_exti_cb_t`→`pl_exti_fn_t`、`pl_net_ip_listener_t`→`pl_net_ip_listener_fn_t`、`pl_tim_period_cb_t`→`pl_tim_period_fn_t`

**Platform —— 类型 / 宏**
- `pl_gpio.h:12`：`pl_port_t`→`pl_gpio_port_t`
- `net_diag.h:35`：`NET_DIAG`→`PL_NET_DIAG`（27 处）
- `pl_eth.c` 宏加前缀：`TIME_WAITING_FOR_INPUT`、`ETHIF_TX_TIMEOUT`、`INTERFACE_THREAD_STACK_SIZE`、`IFNAME0/1`、`ETH_TX_BUFFER_MAX`
- `boards/3833024/Platform/Inc/pl_hub75.h`：`hub75_pin_t`→`pl_hub75_pin_t`；**整体删除死代码**——颜色类型 + 8 个 `HUB75_COLOR_*` 常量 + 零引用的 `pl_hub75_set_rgb` 内联（已执行）。保留 `pl_hub75_pin_t` 与 `g_hub75_pin_r/g/b`（被三个显示驱动用于构建 `s_bsrr_table`，是活代码）

**Platform —— `g_`→`s_` 与自注释（§4.6/§5）**
- `pl_eth.c`：`g_phy_get_link`→`s_phy_link_fn`、`g_eth_hw_ready`→`s_eth_hw_ready`、`RxAllocStatus`→`s_rx_alloc_status`、`DMARxDscrTab`→`s_dma_rx_desc_tab`、`DMATxDscrTab`→`s_dma_tx_desc_tab`、`RxPktSemaphore`→`s_rx_pkt_sem`、`TxPktSemaphore`→`s_tx_pkt_sem`、`TxConfig`→`s_tx_config`、`RxAllocStatusTypeDef`→`rx_alloc_state_t`、`RxBuff_t`→`rx_buff_t`
- `pl_uart.c`：`g_uart_ctx`→`s_uart_ctx`、`PLU_LOG`→`PL_UART_LOG`
- `pl_spi.c`：`g_spi_ctx`→`s_spi_ctx`
- `pl_exti.c`：`s_cb`→`s_exti_cb_table`
- `pl_tim.c`：`g_period_cb`→`s_period_cb`
- `pl_net.c`：`g_ip_listeners`→`s_ip_listeners`、`g_new_ip/mask/gw`→`s_new_ip/mask/gw`、`IP_ADDRESS/NETMASK_ADDRESS/GATEWAY_ADDRESS`→`s_ip_bytes/s_netmask_bytes/s_gateway_bytes`、`attributes`→`s_net_thread_attr`、`gnetif`→`g_net_netif`；`ipaddr`/`netmask`/`gw` 三个全局**降为 `pl_net_init` 内局部变量**（全局消失）
- `pl_flash.c`：`g_voltage_map`→`s_voltage_map`
- `pl_gpio.c`：`g_port_base`→`s_port_base`

**Platform —— 静态函数补 `_`**
- `pl_eth.c`：`low_level_init`→`_eth_low_level_init`、`low_level_output`→`_eth_low_level_output`、`low_level_input`→`_eth_low_level_input`、`low_level_output_arp_off`→`_eth_low_level_output_arp_off`
- `pl_uart.c`：`uart_idle_handle`→`_uart_idle_handle`、`pl_uart_tx_sync_init`→`_uart_tx_sync_init`、`_wire_ms`→`_uart_wire_ms`、`_rx_rearm`→`_uart_rx_rearm`
- `pl_net.c`：`apply_ip_config`→`_net_apply_ip_cfg`

**Platform —— 导出函数补层前缀（§3.1）**
- `pl_eth.h`：`ethernetif_init`→`pl_eth_netif_init`、`ethernetif_input`→`pl_eth_netif_input`、`ethernet_link_thread`→`pl_eth_link_task`
- **豁免**（§14 E4）：`sys_now`、`sys_jiffies`、`heth`

**Platform —— 删除重复定义（§7.4）**
- `pl_hub75.h:50-57` 的 `HUB75_COLOR_*`：**已整体删除**（连同颜色类型与零引用的 `pl_hub75_set_rgb`）。⚠️ 原方案"与 Device 侧 `dev_display_color_t` 归一为一套"**不成立**——Platform 不能依赖 Device（依赖方向严格单向），Platform 必须自持其硬件描述

### B1-B Device + boards（约 300 行）

**基成员与转型（§8.4）**
- 成员 `me`→`base`：`Device/Inc/dev_flash_int.h:17`、`Device/Storage/dev_w25qxx.c:28`、`boards/3833024/Device/Src/dev_P10_32x16_2200001703.c:38`、`dev_p20_16x16_1000001055.c:56`、`dev_p20_16x8_2200001667.c:72`（`boards/5006048/.../dev_p10_112x10_1000000661.c:136` 已是 `base`，跳过）
- 裸转型→`container_of`：`dev_w25qxx.c:137,173,261,315`（`(dev_w25qxx_t *)dev`）、`dev_flash_int.c:25,32,54`（`(dev_flash_int_t *)dev`）
- 同步各文件内所有 `->me`/`.me` 引用；板级三个 display 文件**无转型点**，仅改成员名

**层前缀（§1/§2.2）**
- `cfg_record.h`：`cfg_record_hdr_t`→`dev_cfg_record_hdr_t`、`cfg_rec_sta_t`→`dev_cfg_record_state_t`、`cfg_record_load/save`→`dev_cfg_record_load/save`、`cfg_record_crc_fn`→`dev_cfg_record_crc_fn_t`、`CFG_REC_OK/EMPTY/INVALID/IO_ERR`→`DEV_CFG_RECORD_STATE_*`（94 行族）
- `config_info.h`：`SysInfo_t`→`dev_sys_info_t`、`FWInfo_t`→`dev_fw_info_t`、`NetConfig_t`→`dev_net_cfg_t`、`UpdateSta_t`→`dev_update_status_t`；裸常量 `updated/updating/failed`→`DEV_UPDATE_STATUS_UPDATED/UPDATING/FAILED`
- `dev_light_sensor.h:24`：`light_sensor_dev_t`→`dev_light_sensor_dev_t`
- `dev_display.h`：`display_color_t`→`dev_display_color_t`、`COLOR_*`→`DEV_DISPLAY_COLOR_*`（48+41 行）
- `flash_int_ops`（`dev_flash_int.h:23` extern）→`g_flash_int_ops`（§8.3 跨文件虚表）

**`g_`→`s_` 归位（§4.6）**
- `g_w25qxx`→`s_w25qxx_dev`、`g_dp83848`→`s_dp83848_dev`、`g_dev_dp83848_io_ctx`→`s_dp83848_io_ctx`、`g_default_ip/mask/gw`→`s_default_ip/mask/gw`、`g_p20`→`s_p20_dev`、`g_bsrr`→`s_bsrr_table`、`gs_bsrr`→`s_bsrr_table`

**虚表实例加 `s_`（§8.3）**
- `w25qxx_ops`→`s_w25qxx_ops`、`exti_key_ops`→`s_exti_key_ops`、`dip_key_ops`→`s_dip_key_ops`、`module_ops`→`s_module_ops`、`p20_ops`→`s_p20_ops`、`ops`(p10_112)→`s_p10_ops`

**无 `s_` 的私有数据（§4.1）**
- `pixel_map`、`hub75_buff`、`hub75_IO`、`channel_map`、`module`、`sec`、`scan_line`、`pwm_cnt`、`applied_level`、`cand_level`、`cand_cnt` → 各自加 `s_`（含函数内 static）

**拼写与模块词**
- `dev_p10_32x16_22200001703_init`→`dev_p10_32x16_2200001703_init`（多打一个 `2`）
- `dev_p10_112x10…:346` `dev_module_init`→`dev_p10_112x10_init`
- `g_board_font`：**待定**（合规存疑，见 §待办）

**不动**：`dev_key_board.c`/`app_test_board.c`（§10.5 板级覆盖范式）、`g_dev_key_board`/`g_dev_key_board_count`（§4.2 合规）、`dev_dp83848_*_fn_t`（§8.1 合规）

### B1-C Application 核心（约 770 行）

**`pcb_`/`ccb_` → `app_*`（§1，v1.1 取消例外）**
- 类型：`ccb_t`→`app_ccb_t`、`pcb_t`→`app_pcb_t`、`ccb_state_t`→`app_ccb_state_t`、`ccb_src_t`→`app_ccb_src_t`、`ccb_dst_t`→`app_ccb_dst_t`、`ccb_ops_t`→`app_ccb_ops_t`、`ccb_notify_t`→`app_ccb_notify_t`、`pcb_ops_t`→`app_pcb_ops_t`、`pcb_probe_fn_t`→`app_pcb_probe_fn_t`、`pcb_probe_sta_t`→`app_pcb_probe_state_t`；`struct ccb`/`struct pcb` tag 同名去 `_t`（§2.4）
- 函数：`ccb_send`→`app_ccb_send`、`ccb_send_to`→`app_ccb_send_to`、`app_proto_bind`→`app_dispatch_bind`
- 常量：`PCB_PROBE_READY/WAIT/FAKE/SKIP`→`APP_PCB_PROBE_STATE_*`、`CCB_STATE_DOWN/UP`→`APP_CCB_STATE_*`、`CCB_NOTIFY_MAX`/`CCB_PROTO_MAX`/`CCB_SRC_TOPIC_MAX`→`APP_CCB_*`
- **已合规不改**：`app_ccb_dispatch`

**调度旧代名（§16）**
- `frame_msg_t`→`app_dispatch_msg_t`（43）、`dispatch_ctx_t`→`app_dispatch_ctx_t`、`dispatch_rx_listener_t`→`app_dispatch_rx_listener_fn_t`、`frame_dispatch_task`→`app_dispatch_task`、`g_dispatch`→`g_dispatch_ctx`
- **不改**：`g_dispatch_task_handle`（末词 `handle` 合规）

**类型前缀（§2.2）**
- `screen_card_t`→`app_screen_card_t`、`screen_card_state_t`→`app_screen_card_state_t`、`screen_layout_t`→`app_screen_layout_t`、`cfg_sched_desc_t`/`cfg_sched_owner_t`→`app_cfg_sched_*`
- `casc_*`→`app_casc_*`：`casc_type_t`、`casc_hdr_t`、`casc_present_t`、`casc_set_bright_t`、`casc_set_addr_t`、`casc_image_t`、`casc_nack_err_t`、`casc_nack_t`、`casc_id_rec_t`、`casc_cmd_fn_t`
- 常量：`SCREEN_CARD_MISSING/ONLINE/OFFLINE`→`APP_SCREEN_CARD_STATE_*`、`SCREEN_CARD_MAX`→`APP_SCREEN_CARD_MAX`、`CASC_T_*`→`APP_CASC_TYPE_*`、`CASC_NACK_*`→`APP_CASC_NACK_*`；宏 `CASC_SOF*/OVERHEAD/FRAME_*/ADDR_*/PROTO_VER/RETRY_MAX`→`CASC_*` 保持前缀（文件改名后模块名仍为 `casc`），逐条 `CASC_`→`APP_CASC_` 由 §6.1「宏随模块名」裁定为 `CASC_*` 不变、仅随文件改名确认
- **不改**：`application.h`（§14 E1 层根聚合头）、`app_diag.c`（全 static、无导出）

**自注释（§5）**
- `g_render_fn`→`s_render_fn_table`、`g_casc_cmd`→`s_casc_cmd_table`、`g_factory_test`→`s_factory_test_task_handle`（均应为文件 static）
- ~40 处 `s_*` 补类别词/限定词：`s_stor`→`s_storage_dev`、`s_owners`→`s_owner_table`、`s_owner_block`→`s_owner_block_map`、`s_scratch`→`s_scratch_buf`、`s_canvas`→`s_canvas_buf`、`s_display`→`s_display_dev`、`s_self`→`s_self_idx`、`s_cards`→`s_card_table`、`s_layout`→`s_screen_layout`、`s_band`→`s_band_buf`、`s_tasks`→`s_task_table`、`s_alarm`→`s_alarm_fn`、`s_rx_listener`→`s_rx_listener_fn`、`s_ack`→`s_ack_state`、`s_tx`→`s_tx_buf`、`s_ping_left`→`s_ping_left_cnt`、`s_gen`→`s_gen_cnt`、`s_ready`/`s_bound`→`s_storage_ready`/`s_storage_bound`、`s_pending`/`s_canvas_touched`/`s_bright_pending`/`s_enum_seen`/`s_force_round`/`s_claim_req`→`*_flag`、`s_enum_deadline`/`s_ping_next`/`s_reenum_at`/`s_bus_quiet_until`→带 `_ms`/`_tick` 后缀（逐项按 §5.1/§5.3/§5.5 推导，执行时全量 grep `s_` 核对）

**静态函数补 `_`（§3.2）**
- `app_boot.c`：`half_sec_task`→`_half_sec_task`、`init_task`→`_init_task`
- `app_factory_test.c`：`factory_monitor_task`→`_factory_monitor_task`
- `app_cascade.c`：`casc_probe_frame`→`_casc_probe_frame`、`casc_task`→`_casc_task`

### B1-D 协议与通道 + test（约 900–1000 行）

**导出函数补 `app_`（§3.1；`handle` 冗余一并删除，§3.6）**
- IAP：`iap_probe_frame`→`app_iap_probe_frame`、`iap_handle_task`→`app_iap_task`
- LDI：`ldi_probe_frame`→`app_ldi_probe_frame`、`ldi_handle_task`→`app_ldi_task`、`ldi_timer_task`→`app_ldi_timer_task`、`ldi_ctx_init`→`app_ldi_ctx_init`、`ldi_build_rsp_head`→`app_ldi_build_rsp_head`、`ldi_build_ctrl_rsp_head`→`app_ldi_build_ctrl_rsp_head`、`ldi_send_cert_req`→`app_ldi_send_cert_req`、`ldi_send_sta_rpt`→`app_ldi_send_state_rpt`、`ldi_get_device_index`→`app_ldi_get_device_idx`、`ldi_set_device_index`→`app_ldi_set_device_idx`
- RLS：`rls_probe_frame`→`app_rls_probe_frame`、`rls_handle_task`→`app_rls_task`
- AHMQ：`ah_mqtt_probe_frame`→`app_ahmq_probe_frame`、`ah_mqtt_handle_task`→`app_ahmq_task`
- Channel：`mqtt_send_data`→`app_mqtt_send`、`mqtt_task`→`app_mqtt_task`、`mqtt_connection`→`app_mqtt_connect`、`tcp_server_task`/`tcp_server_conn_task`→`app_tcp_server_*`、`tcp_client_task`/`tcp_client_conn_task`→`app_tcp_client_*`、`udp_task`/`udp_connect_task`→`app_udp_*`
- VMS（LDI 子模块）：`vms_ctrl`→`app_vms_ctrl`、`vms_timer_poll`→`app_vms_timer_poll`

**导出类型补 `app_`（§2.2）**
- LDI：`ldi_ctx_t` `ldi_state_t` `ldi_device_t` `ldi_frame_t` `ldi_req_head_t` `ldi_ctrl_head_t` `ldi_cmd_type_t` `ldi_module_head_t` `ldi_cfg_*_t`(5) `ldi_ctrl_*_t`(9) `ldi_reboot_payload_t` `ldi_device_info_t` `ldi_cert_req_t` `ldi_sta_rpt_t`(→`app_ldi_state_rpt_t`) → `app_ldi_*`
- RLS：`rls_frame_t` `rls_cmd_type_t` `rls_dispaly_t`(→`app_rls_display_t`，拼写) → `app_rls_*`
- MQTT：`mqtt_state_t` `mqtt_ctx_t` `mqtt_ccb_t` → `app_mqtt_*`；`tcp_ccb_t`/`udp_ccb_t` → `app_tcp_ccb_t`/`app_udp_ccb_t`
- AHMQ：`ah_mqtt_proto_t` `topic_info_t` `notify_date_t` `notify_id_t` `state_report_t` `sign_up_t` `cmd_display_t` `cmd_fill_t` `cmd_restart_t` `cmd_checktime_t` → `app_ahmq_*`
- IAP：`iap_frame_t`→`app_iap_frame_t`、`rtn_cmd_t`→`app_iap_rtn_cmd_t`
- handler typedef 加层前缀：`iap_cmd_handler_fn_t`/`ldi_cmd_handler_fn_t`/`rls_cmd_handler_fn_t`/`ah_mqtt_cmd_handler_fn_t`→`app_*`
- **不改**：`app_flash_iap_*`、`app_flash_ldi_*`（已带层前缀）

**extern 缺 `g_`（§4.2）**
- `mqtt_task_handle`→`g_mqtt_task_handle`、`mqtt_ccb_ops`→`g_mqtt_ccb_ops`、`tcp_server_task_handle`→`g_tcp_server_task_handle`、`tcp_ccb_ops`→`g_tcp_ccb_ops`、`tcp_client_task_handle`→`g_tcp_client_task_handle`、`udp_task_handle`→`g_udp_task_handle`、`udp_ccb_ops`→`g_udp_ccb_ops`
- `ProtocolTask_attributes`→`g_ahmq_task_attr`；`ah_mqtt.c` 私有 `SignUpHandle`/`ReportHandle`/`SignUpTask_attributes`/`ReportTask_attributes` 同规则
- `*_task_attr` 一批（`iap_task_attr`/`ldi_task_attr`/`rls_task_attr`/`mqtt_task_attr`/`tcp_server_task_attr`/`tcp_client_task_attr`/`udp_task_attr`）→ 跨文件 `const`，按 §4.2 一律 `g_*_task_attr`

**自注释（§5）**
- `g_proto_ah_matt_queue`→`g_ahmq_msg_queue`（含拼写修正）
- `g_ldi`→`g_ldi_ctx`（115）、`g_mqtt`→`g_mqtt_ccb`（29）、`g_config`→`g_iap_sys_info`
- 文件 static 误用 `g_`：`g_flash_iap`→`s_flash_iap`、`g_tcp_server`→`s_tcp_server_ccb`、`g_port`→`s_port`、`g_tcp_client`→`s_tcp_client_ccb`、`g_udp_port`→`s_udp_port`、`g_udp`→`s_udp_ccb`、`g_rs485`→`s_rs485_ccb`、`g_rs232_0/1`→`s_rs232_0/1`

**静态函数补 `_` / `_cb`→`_fn`（§3.2/§9.3）**
- `app_iap_cmd.c`：`cmd_Test_00`…`cmd_Restart_07`、`cmd_SendReData` → `_iap_cmd_test`、`_iap_cmd_report_ip`、`_iap_cmd_force_modify_ip`、`_iap_cmd_report_fw_status`、`_iap_cmd_prepare_upgrade`、`_iap_cmd_send_upgrade_pkg`、`_iap_cmd_enter_recovery`、`_iap_cmd_restart`、`_iap_cmd_send_re_data`
- `app_ldi_cmd.c`：`ldi_cfg_module_size`/`ldi_next_rpt_seq`/`ldi_send_response` 及 `cmd_*`(13) → `_ldi_*`
- `app_rls_cmd.c`：`cmd_test`/`cmd_display`/`cmd_display_save` → `_rls_cmd_*`
- `app_vms_ctrl.c`：`vms_clear_screen`/`vms_display_ctrl`/`vms_clean_ctrl` → `_vms_*`
- `app_mqtt.c`：`mqtt_send`→`_mqtt_send`；`mqtt_channel_init/deinit`、`mqtt_*_cb`→`_mqtt_*` 且 `_cb`→`_fn`
- `app_tcp_server.c`：`tcp_send`→`_tcp_send`；`app_udp.c`：`udp_ccb_send`→`_udp_send`
- `boards/3833024/.../app_rs232.c`：`rs232_send`/`rs232_isr_cb`(→`_rs232_isr_fn`)/`rs232_task`/`rs232_start`/`rs232_channels_start` → `_rs232_*`
- `app_rs485.c`：`rs485_send`→`_rs485_send`、`rs485_isr_cb`→`_rs485_isr_fn`

**枚举（§7）**
- `rtn_cmd01..07` → `APP_IAP_RTN_CMD_01..07`（类型 `app_iap_rtn_cmd_t`）
- `LDI_CMD_*`→`APP_LDI_CMD_TYPE_*`、`LDI_DEV_TYPE_*`→`APP_LDI_DEVICE_*`、`LDI_ST_*`→`APP_LDI_STATE_*`、`MQTT_ST_*`→`APP_MQTT_STATE_*`、`RLS_CMD_*`→`APP_RLS_CMD_TYPE_*`

**test/ 同步**
- `test_probes.c`（32 行）：探针名 + `g_ldi`；`test_ldi_0ah.c`（7 行）：`g_ldi`、`vms_ctrl` 桩；其余测试无引用

---

### B1 补漏清单（三路 fixer 上报的缺口；均为映射表未列到、但按约定必须改）

**Platform**

| # | 现状 | 目标 | 备注 |
|---|---|---|---|
| P1 | `pl_eth.c` `TIME_WAITING_FOR_INPUT` | `ETH_INPUT_WAIT_TICKS` | 值是 `portMAX_DELAY`，是 tick 不是毫秒 |
| P2 | `ETHIF_TX_TIMEOUT` | `ETH_TX_TIMEOUT_MS` | 值 2000，补单位 |
| P3 | `pl_eth.c` `INTERFACE_THREAD_STACK_SIZE`(256*4) | `ETH_IF_TASK_STACK` | ⚠️ 与 P4 同名不同值 |
| P4 | `pl_net.c` `INTERFACE_THREAD_STACK_SIZE`(1024) | `ETH_NET_TASK_STACK` | 二者必须区分 |
| P5 | `IFNAME0`/`IFNAME1` | `ETH_IFNAME0`/`ETH_IFNAME1` | |
| P6 | `ETH_DMA_TRANSMIT_TIMEOUT` | `ETH_DMA_TX_TIMEOUT_MS` | |
| P7 | `NET_DIAG_ENABLE` | `PL_NET_DIAG_ENABLE` | |
| P8 | `RX_ALLOC_OK`/`RX_ALLOC_ERROR` | `RX_ALLOC_STATE_OK`/`RX_ALLOC_STATE_ERROR` | 类型已是 `rx_alloc_state_t` |
| P9 | `pbuf_free_custom` | `static _pbuf_free_custom` | 仅本文件用（函数指针） |
| P10 | `pl_uart_set_rx_cb` + 成员 `rx_cb`/`rx_cb_ctx` | `pl_uart_set_rx_fn` + `rx_fn`/`rx_fn_ctx` | §9.3 `cb`→`fn` |
| P11 | `pl_spi_set_rx_cplt_cb` + 成员 `rx_cplt_cb`/`rx_cplt_ctx` | `pl_spi_set_rx_done_fn` + `rx_done_fn`/`rx_done_ctx` | |

**Device**

| # | 现状 | 目标 |
|---|---|---|
| D1 | `config_info.h` 成员 `update_sta` | `update_status` |
| D2 | `config_info.h` 5 个函数（`Is_Config_Empty`/`Init_Config_Info`/`Edit_Config_Info`/`EraseConfigInfo`/`WriteConfigInfo`） | **删除声明**（零定义、零调用；执行前复核） |
| D3 | `dev_display.c` 静态 `scan_task` | `_scan_task` |
| D4 | 5006048 P10 驱动 `convert_pixelmap`/`prepare_send_buffer`/`build_channel_table` | `_convert_pixelmap`/`_prepare_send_buffer`/`_build_channel_table` |
| D5 | `p20_pixel_map`/`p20_hub75_buff` | `s_pixel_map`/`s_hub75_buff` |
| D6 | `s_hub75_IO` | `s_hub75_io`（非宏标识符不用大写） |

**Application**

| # | 现状 | 目标 |
|---|---|---|
| A1 | `g_board_font` | `g_board_font_lib`（含 `boards/*/Application/Src/font_lib_board.c` 与 `app_render.h/.c`、`test/test_font_lib.c`） |
| A2 | 导出类型 `render_target_t` `render_cfg_t` `render_style_t` `render_type_t` `render_fn_t` `render_persist_t` `render_persist_hook_t` | `app_render_*`（`render_persist_hook_t` 是函数指针 → `app_render_persist_hook_fn_t`） |
| A3 | 导出类型 `font_lib_desc_t` `font_unit_t` `font_key_t` `font_size_t` `font_type_t` `font_enc_t` `font_idx_kind_t` | `app_font_*`（`font` 登记为模块名） |
| A4 | `align_t` | `app_render_align_t`（定义在 `app_render.h`） |
| A5 | 静态函数漏 `_`（含头内 `static inline`）：`iap_module_init`、`iap_ip_change_cb`、`ldi_module_init`、`ldi_ctrl_payload_size`、`rls_module_init`、`ah_mqtt_module_init`、`ah_mqtt_topic_index`、`SignUpTask`、`ReportTask`、`rs485_task`、`tcp_keepaliveinit`、`casc_get_u16`/`casc_put_u16`/`casc_get_u32`/`casc_put_u32`、`ah_mqtt_cmd.c` 的 `cmd_display`/`cmd_fill`/`cmd_restart`/`cmd_checktime` | 加 `_` 前缀（`_iap_module_init` 等） |
| A6 | 文件 static/ops 缺 `s_`：`rs485_ccb_ops`、`rs232_ccb_ops`、`client_disconnect_sem`、`tcp_client_conn_attr`、`udp_disconnect_sem`、`udp_connect_attr`、`tcp_server_conn_attr`、`g_tcp_server_connected` | 加 `s_` 前缀 |
| A7 | AHMQ struct tag 与 typedef 不同名（`topic_baseinfo`/`notify_date`/`notify_id`/`state_report`/`sign_up`/`cmd_disp`/`cmd_fill`/`cmd_restart`/`cmd_checktime`） | tag 取 typedef 去 `_t`（§2.4） |
| A8 | `g_ah_mqtt_task_handle` | `g_ahmq_task_handle` |

**执行方式**：一轮 fixer 完成；门槛与 B1 相同（`make test` + 两个固件目标）。

---

## B2 文件 / 目录级

**B1 残留收尾（§9.3，活代码）**

| 现状 | 目标 | 位置 |
|---|---|---|
| `app_flash_iap_update_sta_t` | `app_flash_iap_update_status_t` | `Application/Inc/IAP/app_iap_cfg.h:34` |
| 成员 `update_sta` | `update_status` | `app_iap_cfg.h:39`、`app_iap_cfg.c`(3)、`app_iap_cmd.c:120`、`test_iap_cfg.c`(5，含报错文案字符串 `"update_sta"`)、`test_ldi_0ah.c:342` |
| 成员 `work_sta`/`run_sta` | `work_status`/`run_status` | `Application/Inc/AH_MQTT/ah_mqtt.h:37,38,46`、`ah_mqtt.c:195,201,221` |

**枚举常量加前缀（§7.2）**

常量 = 枚举类型名（含层前缀）去 `_t` 后全大写 + `_值`。故 `FONT_*`/`FONT_ENC_*`/`FONT_IDX_*`/`FONT_SIZE_*`/`FONT_TYPE_*` → `APP_FONT_*`；`RENDER_*` → `APP_RENDER_*`；`ALIGN_*` → `APP_RENDER_ALIGN_*`。以实际类型名推导为准，执行时全量 grep 核对。

**目录改名（§10.2）**
- `Application/{Inc,Src}/CASCADE/` → `.../CASC/`；`Application/{Inc,Src}/AH_MQTT/` → `.../AHMQ/`
- 同步：`Makefile`（`57,316,434` 等）与 `.eide/eide.yml` 两目标的 include/源清单

**文件改名（§10.1/§10.5）**
- `app_cascade.c/h` → `app_casc.c/h`；`test_cascade_*.c` → `test_casc_*.c`
- `ah_mqtt.c/h`、`ah_mqtt_cmd.c/h` → `app_ahmq.c/h`、`app_ahmq_cmd.c/h`
- ~~`Device/Inc/config_info.h`~~：**已删除**（整份死代码——无人 `#include`、地址宏与 `app_iap_cfg.h:8-10` 完全重复、`CONFIG_MAGIC` 零使用、其余类型/枚举仅自用）
- `Device/Inc/cfg_record.h` → `dev_cfg_record.h`
- `boards/3833024/Device/Src/dev_P10_32x16_2200001703.c` → `dev_p10_32x16_2200001703.c`（大小写）
- `boards/*/Application/Src/font_lib_board.c` → `app_font_lib_board.c`
- `Platform/Inc/net_diag.h` → `pl_net_diag.h`（同步 5 处 include）

**`Device/Inc` 镜像 `Device/Src`（§10.3，已裁决方案 A）**
- `Device/Inc/Display/`：`dev_display.h`
- `Device/Inc/IO/`：`dev_key.h`、`dev_light_sensor.h`
- `Device/Inc/Network/`：`dev_dp83848.h`、`dev_eth.h`
- `Device/Inc/Storage/`：`dev_storage.h`、`dev_w25qxx.h`、`dev_flash_int.h`、`dev_cfg_record.h`
- 留层根：无需保留的契约头（原 `config_info.h` 已删除）
- 无需改任何 `#include`（各子目录加入 `-I`）；需在 `Makefile`（固件+测试）与 `.eide` 两目标各加 4 条 `-I`

**头保护（§11）**
- 补：`Kernel/Inc/bcc_utils.h`
- 转：`Compiler/startup.h`、`Device/Inc/dev_dp83848.h`、`Platform/Inc/pl_eth.h`
- 豁免：`Platform/Inc/lwipopts.h`（§14 E2）

---

## 禁改与豁免（改名 ≠ 契约改名）

- 持久化记录 `name[16]` 归属串（如 `"casc_id"`）、记录头布局 → **任何改名不得触碰**；`casc_id` 若因目录改名需迁移，须同时升 version
- 协议帧字段名、命令码、帧定界符、校验常量**数值**、`CASC_SOF*` 等线格式值
- 链接脚本符号与段名（`.hw_initcall`/`.sw_initcall`）、`board.mk`/`Makefile` 目标名
- `Drivers/`、`Middlewares/`、`boards/*/Core/`（CubeMX）
- 工具链/库固定名：`main`、`Reset_Handler`、`*_IRQHandler`、`HAL_*_Callback`、`sys_now`、`sys_jiffies`、`heth`、newlib 桩
- `container_of`（行业通用，32 处跨层使用，不改）
- `ring_buffer_t`/`rb_*`/`crc*`/`bit_ctz`（Kernel 已合规）

---

## 顺带修复（非命名，独立提交）

1. `app_ldi_cmd.c:421 cmd_set_ip` 漏 `static` → 补 `static`（除非确为对外 API）
2. `config_info.h:43-47` 5 个无定义无调用的声明 → 删除（或补实现，待定）
3. 拼写：`g_proto_ah_matt_queue`(matt)、`rls_dispaly_t`(dispaly)、`bcc_calcu`(calcu)、`dev_p10_32x16_22200001703_init`(多一个 2)
4. `pl_hub75.h` 的 `HUB75_COLOR_*` 零引用 → 删除
5. §4.7 跨模块直取全局：`app_ldi.c:108-111` 读 IAP 的 `g_config` → IAP 提供访问器 API（如 `app_iap_sys_info_get()`）

---

## 验证协议

**B1**：`grep -rn '<旧名>'` 清零 → `make test` 15 套全绿
**B2**：`make -j8`（3833024）与 `make BOARD=5006048` 各编一次 → EIDE 两个目标各编一次 → `make test` 仍全绿

**已知 `test/` 需同步的改名点**：`test_probes.c`、`test_ldi_0ah.c`、`test_cascade_{frame,master,round}.c`（含 `#define BOARD_CASCADE_ENABLED 1` 与 `#include "../Application/Src/CASCADE/app_cascade.c"` 绝对路径）、`test_iap_cfg.c`（`CFG_REC_*`）、`test_dispatch.c`（`ccb`/`frame_msg_t`）

---

## 人工工作项：编码与换行符（由使用者用 VSCode 转换）

**执行时机**：等 B2 文件改名完成、所有代码改动落定之后再做，并**单独提交**（避免与改名 diff 混在一起）。这两个 kernel 文件刚被 B1-A 改过符号，`config_info.h` 将被 B2 改名，提前转换会反复。

### A. 非 UTF-8（需转 UTF-8）

| 文件 | 现状 |
|---|---|
| ~~`Device/Inc/config_info.h`~~ | 已转 UTF-8 后**随 B2 删除**（整份死代码，见 B2 节） |
| `Kernel/Inc/text_cvt.h` | ISO-8859/GBK，CRLF |
| `Kernel/Src/text_cvt.c` | ISO-8859/GBK，CRLF（约 11,334 行） |

**VSCode 步骤**：打开文件 → 状态栏点编码 → **Reopen with Encoding → Chinese (GBK)** → **确认正文显示正常**（不是乱码）→ 再点编码 → **Save with Encoding → UTF-8** → 状态栏点 `CRLF` → **LF** → 保存。

⚠️ 务必先确认"重开后的正文可读"再另存；顺序颠倒会造成二次乱码。

### B. 已是 UTF-8，但换行符为 CRLF（只需转 LF）

| 文件 | CRLF 行数 |
|---|---|
| `Compiler/startup.c` | 145 |
| `Compiler/startup.h` | 123 |
| `Platform/Inc/lwipopts.h` | 132 |
| `Kernel/Inc/ring_buffer.h` | 94 |
| `Platform/Inc/pl_eth.h` | 62 |
| `boards/5006048/Device/Src/dev_rs485.c` | 35 |
| `boards/5006048/Device/Inc/dev_rs485.h` | 10 |

**VSCode 步骤**：状态栏点 `CRLF` → 选 **LF** → 保存。

### 建议顺带加 `.gitattributes`（防复发）

```
* text=auto eol=lf
*.hex -text
*.bin -text
```

否则以后在 Windows/其它编辑器里一碰又会写回 CRLF。

---

## 待办（执行时确认）

| 项 | 结论 |
|---|---|
| `app_proto_bind` → `app_dispatch_bind` | **已定**（方向：现状 `app_proto_bind` → 目标 `app_dispatch_bind`） |
| `g_board_font` → `g_board_font_lib` | **已定**（补类别词） |
| `flash_int_ops` → `g_flash_int_ops` | **已定**：跨文件（IAP 与两个测试都在用）→ `g_` 正确；§4.7 已补豁免「const 能力表不受跨模块限制」 |
| `casc_id` 持久化键 | **无需迁移**：只改 C 符号名，字符串值 `"casc_id"` 保持不变（§13 禁改）；若把字符串值也改掉才需迁移+升 version |
| `pl_net.c` 的 `ipaddr`/`netmask`/`gw` | **非 LwIP 宏**，仅 `pl_net_init` 内使用（`pl_net.c:66-71`）→ **降为函数内局部变量**，全局消失（列入「顺带修复」） |
| `pl_net.c` 的 `IP_ADDRESS`/`NETMASK_ADDRESS`/`GATEWAY_ADDRESS` | 字节数组，被 `pl_net_set_ip`/`pl_net_get_ip` 使用 → 文件私有 → `s_ip_bytes`/`s_netmask_bytes`/`s_gateway_bytes` |
| `LDI_CMD_TYPE_*` 等长常量名 | **已定**：按 §7.2 机械推导，常量随枚举类型名加前缀（`LDI_CMD_*`→`APP_LDI_CMD_TYPE_*`） |
